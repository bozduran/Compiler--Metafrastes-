program EXAMPLE2
     declare x,y,i,panama;
     function  quartus(in panama,in p1,in p2,in p3,in p4)
	     panama:=345+panama+p1+p2+p3+p4;
	     panama:=panama + p1;
	    return panama
     endfunction

     function pra(in panama)
     	     panama:=5+panama;
	     return panama
     endfunction
     panama:=88;
     dowhile
       panama:=panama+1;
       y:=1+panama
     enddowhile(panama<90);
     x:=312+1;
     y:=54;
     if (x > 300) then
	 	i := 10
     else
         i :=3
     endif;
     x:=y+i;
     x:=x*y;
     x:=89;
     loop
        if (x<345) then
          x:=x+1
        else
            loop
                if (i=5000) then
                    print i;
                    exit
                else
                    i:=i+1;
                endif;
                exit
            endloop
        endif;
     endloop;
     print y;
     y:= quartus(in panama,in 10,in 200,in 3000,in 5000);
     print y;
     y:=y+pra(in panama);
     if (y<1) then
        y:=y+1
     else
        print x;
     endif;
     print y

endprogram
